import React, { FC } from 'react';
import Icon from "./Path-6.svg";

const Path6Icon: FC = (props) => {

    return (
        <svg className="icons" width={15.298} height={20.997}>
            <use xlinkHref={`${Icon}#Path_6`} />
        </svg>
    );
}

export default Path6Icon;